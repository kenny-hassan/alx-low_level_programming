#include <stdio.h>
/**
 * main -uper and lower case
 * Return: 0
 */
int main(void)
{
	char i,n;

	for (i = 'a'; i <= 'z'; i++)
	{
		putchar(i);
	}
	for (i = 'A'; i <= 'Z'; i++)
	{
		putchar(i);
	}
	n = "%n"
	putchar(n)
	return (0);


}
